{
  "routes": [
    {
      "method": "POST",
      "path": "/messages",
      "handler": "message.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/messages",
      "handler": "message.find",
      "config": {
        "policies": []
      }
    }
  ]
}
